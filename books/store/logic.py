# Простой файл для примера тестирования через unittest
def operations(a: int, b: int, c: str):
    if c == "+":
        return a + b
    if c == "-":
        return a - b
    if c == "*":
        return a * b
